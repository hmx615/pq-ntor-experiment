# 🚀 PQ-Tor 5节点网络部署方案

**硬件**: 5 × 飞腾派 (Phytium Pi - ARM64)
**网络**: 局域网路由器组网
**日期**: 2025-11-03

---

## 📊 网络拓扑设计

### 节点角色分配

```
局域网: 192.168.1.0/24 (示例)

┌─────────────────────────────────────────────────────────────┐
│                         路由器                               │
│                    192.168.1.1                              │
└─────────────────────────────────────────────────────────────┘
         │           │           │           │           │
    ┌────┴───┐  ┌───┴────┐  ┌───┴────┐  ┌───┴────┐  ┌───┴────┐
    │  派1   │  │  派2   │  │  派3   │  │  派4   │  │  派5   │
    │ Dir +  │  │ Guard  │  │ Middle │  │ Exit   │  │ Client │
    │ Test   │  │  Node  │  │  Node  │  │  Node  │  │        │
    └────────┘  └────────┘  └────────┘  └────────┘  └────────┘
   .10:5000      .11:6001    .12:6002    .13:6003    .14
```

### 节点详细配置

| 节点 | IP地址 | 角色 | 端口 | 功能描述 |
|------|--------|------|------|---------|
| **派1** | 192.168.1.10 | Directory + Test Server | 5000 (Dir)<br>8000 (Test) | • 目录服务器（节点注册/发现）<br>• 测试HTTP服务器（Exit节点访问目标） |
| **派2** | 192.168.1.11 | Guard Node | 6001 | • 入口节点<br>• 接受客户端连接<br>• 电路第1跳 |
| **派3** | 192.168.1.12 | Middle Node | 6002 | • 中间节点<br>• 只做转发<br>• 电路第2跳 |
| **派4** | 192.168.1.13 | Exit Node | 6003 | • 出口节点<br>• 访问目标服务器<br>• 电路第3跳 |
| **派5** | 192.168.1.14 | Client | - | • 客户端程序<br>• 发起匿名请求<br>• 性能测试工具 |

---

## 🔄 通信流程示例

### 完整的3跳匿名HTTP请求

```
[派5 Client]
    │ 1. 从Directory获取节点列表
    ↓
[派1 Directory] → 返回: Guard(派2), Middle(派3), Exit(派4)

[派5 Client]
    │ 2. 建立到Guard的连接
    ↓
[派2 Guard] ←─── TCP连接 ───┐
    │                        │
    │ 3. CREATE2 (PQ-Ntor)   │
    ├──────────────────────→│ [层1密钥建立]
    │ CREATED2               │
    │←───────────────────────┤
    │                        │
    │ 4. EXTEND2 → Middle    │
    │    (加密层1)           │
    ├──────────────────────→│
    │                  解密层1
    │                        ↓
    │              [派3 Middle] ←─── CREATE2 (PQ-Ntor) ───┐
    │                        │                             │
    │                        │ [层2密钥建立]               │
    │                        ├───────────────────────────→│
    │                        │ CREATED2                    │
    │                        │←────────────────────────────┤
    │              加密层1 ←─┘
    │ EXTENDED2              │
    │←───────────────────────┤
    │                        │
    │ 5. EXTEND2 → Exit      │
    │    (加密层1+2)         │
    ├──────────────────────→│
    │                  解密层1
    │                        ↓
    │              [派3 Middle]
    │                  转发(层2加密)
    │                        ↓
    │                        │
    │              [派4 Exit] ←─── CREATE2 (PQ-Ntor) ───┐
    │                        │                            │
    │                        │ [层3密钥建立]              │
    │                        ├──────────────────────────→│
    │                        │ CREATED2                   │
    │                        │←───────────────────────────┤
    │              回传(层2加密)
    │                        ↓
    │              [派3 Middle]
    │                  加密层1
    │                        ↓
    │ EXTENDED2              │
    │←───────────────────────┤
    │
    │ 6. RELAY_BEGIN         │
    │    (加密层1+2+3)       │
    ├──────────────────────→│
    │                  解密层1, 转发
    │                        ↓
    │              [派3 Middle]
    │                  解密层2, 转发
    │                        ↓
    │              [派4 Exit]
    │                  解密层3
    │                        ↓
    │              连接到 派1:8000
    │                        │
    │              [派1 Test Server]
    │                        │
    │              HTTP响应 ←┘
    │                        │
    │ RELAY_DATA             │
    │ (逐层加密回传)         │
    │←───────────────────────┤
    ↓
[派5 Client] 收到匿名响应 ✅
```

---

## 💻 程序模块设计

### 共享库 (所有节点)

```c
// 核心模块 (已完成)
pq_ntor.c          # PQ-Ntor握手协议
kyber_kem.c        # Kyber KEM封装
crypto_utils.c     # HMAC, HKDF, SHA256

// 新增模块
cell.c             # Cell格式处理
onion_crypto.c     # 洋葱加密/解密
network.c          # TCP通信层
circuit.c          # 电路管理
relay.c            # RELAY消息处理
```

### 派1: Directory Server + Test Server

```c
// directory_server.c
功能:
  - HTTP API: GET /nodes → 返回节点列表JSON
  - 节点注册: POST /register (可选，简化版可硬编码)
  - 节点心跳: POST /heartbeat (可选)

数据结构:
  struct node_entry {
      char name[32];           // "guard", "middle", "exit"
      char ip[16];             // "192.168.1.11"
      uint16_t port;           // 6001
      uint8_t identity[32];    // 节点ID
      uint8_t ntor_key[32];    // Kyber公钥
      uint8_t flags;           // Guard|Middle|Exit
  };

// test_http_server.c
功能:
  - 简单HTTP服务器 (端口8000)
  - 返回测试响应 (用于验证匿名访问)
  - 记录访问日志 (验证来源是Exit节点)
```

### 派2/3/4: Relay Node (Guard/Middle/Exit)

```c
// relay_node.c - 统一的中继节点程序
功能:
  - 启动时从配置加载角色 (Guard/Middle/Exit)
  - 监听端口接受连接
  - 处理 CREATE2 (建立电路)
  - 处理 EXTEND2 (扩展电路)
  - 处理 RELAY cells (数据转发)
  - Exit节点额外: 代理连接到目标服务器

命令行参数:
  ./relay_node --role guard --port 6001 --identity guard.key
  ./relay_node --role middle --port 6002 --identity middle.key
  ./relay_node --role exit --port 6003 --identity exit.key

关键函数:
  - handle_create2()      # 响应握手请求
  - handle_extend2()      # 扩展电路到下一跳
  - handle_relay_data()   # 转发数据
  - handle_relay_begin()  # Exit节点建立目标连接
```

### 派5: Client

```c
// pq_tor_client.c
功能:
  - 从Directory获取节点列表
  - 建立3跳电路 (Guard→Middle→Exit)
  - 发送HTTP请求 (通过RELAY_BEGIN + RELAY_DATA)
  - 接收响应

使用示例:
  ./pq_tor_client --request "http://192.168.1.10:8000/test"

输出:
  [1/3] Connecting to Guard (192.168.1.11:6001)...
  [1/3] CREATE2 handshake with Guard... OK (25.3ms)
  [2/3] EXTEND2 to Middle (192.168.1.12:6002)...
  [2/3] Handshake with Middle... OK (26.1ms)
  [3/3] EXTEND2 to Exit (192.168.1.13:6003)...
  [3/3] Handshake with Exit... OK (24.8ms)

  Circuit established! Total time: 76.2ms

  Sending request through circuit...
  Response received (147 bytes):
  HTTP/1.1 200 OK
  Content-Type: text/plain

  Hello from PQ-Tor! Your request was anonymized.
```

---

## 🛠️ 开发和部署流程

### 阶段1: 本地开发 (WSL)

```bash
# 1. 在现有项目中添加新模块
cd ~/pq-ntor-experiment/c

# 2. 开发新模块
src/cell.c
src/onion_crypto.c
src/network.c
src/circuit.c
src/relay.c

# 3. 开发各节点程序
apps/directory_server.c
apps/relay_node.c
apps/pq_tor_client.c
apps/test_http_server.c

# 4. 本地测试 (模拟5节点)
make test-network
```

### 阶段2: ARM交叉编译

```bash
# 安装ARM交叉编译工具链
sudo apt-get install gcc-aarch64-linux-gnu

# 编译liboqs for ARM64
# (或在飞腾派上直接编译)

# 交叉编译项目
make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu-
```

### 阶段3: 部署到飞腾派

```bash
# deploy.sh - 自动部署脚本
#!/bin/bash

NODES=(
  "pi1:192.168.1.10:directory_server,test_http_server"
  "pi2:192.168.1.11:relay_node"
  "pi3:192.168.1.12:relay_node"
  "pi4:192.168.1.13:relay_node"
  "pi5:192.168.1.14:pq_tor_client"
)

for node in "${NODES[@]}"; do
  IFS=: read -r name ip programs <<< "$node"
  echo "Deploying to $name ($ip)..."

  # 复制二进制文件
  scp build/arm64/* pi@$ip:/home/pi/pq-tor/

  # 复制配置文件
  scp configs/$name.conf pi@$ip:/home/pi/pq-tor/config.conf

  # 复制liboqs
  scp liboqs-arm64/lib/* pi@$ip:/usr/local/lib/
done
```

### 阶段4: 启动网络

```bash
# 启动顺序脚本 start_network.sh

# 1. 派1: 启动目录服务器
ssh pi@192.168.1.10 "cd pq-tor && ./directory_server &"
ssh pi@192.168.1.10 "cd pq-tor && ./test_http_server &"

# 2. 派2-4: 启动中继节点
ssh pi@192.168.1.11 "cd pq-tor && ./relay_node --role guard &"
ssh pi@192.168.1.12 "cd pq-tor && ./relay_node --role middle &"
ssh pi@192.168.1.13 "cd pq-tor && ./relay_node --role exit &"

# 等待节点启动
sleep 3

# 3. 派5: 运行客户端测试
ssh pi@192.168.1.14 "cd pq-tor && ./pq_tor_client --request 'http://192.168.1.10:8000/test'"
```

---

## 📊 性能测试方案

### 测试场景

**1. 握手性能 (单跳)**
- 测量: CREATE2/CREATED2 延迟
- 节点: 每个中继节点独立测试
- 指标: 平均/中位数/P99延迟

**2. 电路建立性能 (3跳)**
- 测量: 完整电路建立时间
- 对比: PQ-Ntor vs 原始Ntor (如果可能)
- 指标: 总延迟 = 握手1 + 握手2 + 握手3 + 网络RTT

**3. 数据传输性能**
- 测量: 端到端延迟和吞吐量
- 测试: 不同大小的数据包 (1KB, 10KB, 100KB)
- 对比: 通过Tor vs 直连

**4. 资源消耗**
- CPU使用率 (每个节点)
- 内存占用
- 网络带宽

### 数据收集

```c
// 在每个节点记录:
struct perf_stats {
    uint64_t handshake_count;
    double handshake_time_sum;
    double handshake_time_min;
    double handshake_time_max;

    uint64_t relay_count;
    uint64_t bytes_forwarded;

    uint64_t circuit_count;
};

// 定期输出到CSV
// pi2_guard_stats.csv
timestamp,handshake_ms,circuits_active,bytes_fwd
1699000000,25.3,5,102400
1699000001,24.1,6,153600
...
```

---

## 🔧 配置文件示例

### 派1: directory.conf
```ini
[directory]
listen_ip = 0.0.0.0
listen_port = 5000
nodes_file = /home/pi/pq-tor/nodes.json

[test_server]
listen_port = 8000
```

### 派2: guard.conf
```ini
[node]
role = guard
listen_port = 6001
identity_key = /home/pi/pq-tor/keys/guard.key
ntor_key = /home/pi/pq-tor/keys/guard_ntor.key

[directory]
server = 192.168.1.10:5000
```

### 派3: middle.conf
```ini
[node]
role = middle
listen_port = 6002
identity_key = /home/pi/pq-tor/keys/middle.key
ntor_key = /home/pi/pq-tor/keys/middle_ntor.key

[directory]
server = 192.168.1.10:5000
```

### 派4: exit.conf
```ini
[node]
role = exit
listen_port = 6003
identity_key = /home/pi/pq-tor/keys/exit.key
ntor_key = /home/pi/pq-tor/keys/exit_ntor.key
allow_exit = true
exit_policy = accept 192.168.1.0/24:8000

[directory]
server = 192.168.1.10:5000
```

### 派5: client.conf
```ini
[client]
directory_server = 192.168.1.10:5000
circuit_length = 3
```

---

## 📋 开发检查清单

### 核心功能
- [ ] Cell格式定义和序列化
- [ ] 洋葱加密/解密 (3层)
- [ ] TCP网络层 (连接管理)
- [ ] 电路管理 (CREATE/EXTEND处理)
- [ ] RELAY消息处理

### 节点程序
- [ ] Directory服务器 (返回节点列表)
- [ ] Relay节点 (统一程序，支持3种角色)
- [ ] 客户端 (建立电路，发送请求)
- [ ] 测试HTTP服务器

### 部署相关
- [ ] ARM64兼容性验证 (liboqs)
- [ ] 交叉编译脚本
- [ ] 部署脚本 (自动化部署到5个派)
- [ ] 配置文件生成器
- [ ] 密钥生成工具

### 测试
- [ ] 单跳握手测试
- [ ] 3跳电路测试
- [ ] 端到端数据传输测试
- [ ] 性能基准测试
- [ ] 故障恢复测试 (节点down掉)

---

## 🎯 时间估计

| 阶段 | 任务 | 估计时间 |
|------|------|---------|
| **1** | Cell + 洋葱加密 | 1天 |
| **2** | 网络层 + 电路管理 | 1-2天 |
| **3** | Directory + Relay节点 | 1-2天 |
| **4** | 客户端程序 | 1天 |
| **5** | ARM移植 + 部署工具 | 1天 |
| **6** | 硬件部署调试 | 1-2天 |
| **7** | 性能测试 | 1天 |
| **总计** | | **7-10天** |

---

## 🎓 论文亮点

使用真实硬件部署的优势:

1. **真实网络环境**
   - 真实的网络延迟和丢包
   - 真实的ARM硬件性能
   - 更可信的实验数据

2. **可重现性**
   - 完整的硬件规格
   - 详细的部署步骤
   - 开源代码和配置

3. **实用性证明**
   - 不仅是理论实现
   - 在资源受限设备 (飞腾派) 上可运行
   - 适合边缘计算场景

4. **对比实验**
   - 可以在同样硬件上运行原始Tor
   - 公平的性能对比

---

## 📝 下一步行动

1. **确认网络配置**
   - 飞腾派的IP地址分配
   - 路由器配置 (静态IP)
   - SSH访问设置

2. **验证ARM兼容性**
   - 在1个飞腾派上编译liboqs
   - 运行现有的PQ-Ntor测试

3. **开始编码**
   - Cell格式处理
   - 洋葱加密模块

需要我开始实现第一个模块吗？或者先在您的飞腾派上验证ARM兼容性？
