# SAGIN 3D地球可视化技术调研报告

> **调研日期**: 2025-11-14
> **调研目的**: 替换当前2D Canvas方案，实现可鼠标拖动旋转的3D地球卫星网络可视化

---

## 📊 技术方案对比

### 方案1: **Cesium.js** ⭐⭐⭐⭐⭐

#### 基本信息
- **官网**: https://cesium.com/platform/cesiumjs/
- **GitHub**: https://github.com/CesiumGS/cesium (10k+ stars)
- **许可证**: Apache 2.0（完全免费，商业可用）
- **定位**: 专业级地理空间可视化引擎

#### 核心优势
1. **原生为卫星设计**
   - 2011年由AGI（Analytical Graphics Inc.）为卫星轨道可视化创建
   - 内置TLE、SGP4轨道计算支持
   - NASA、SpaceX、AGI等航天机构在使用

2. **时间动态系统**
   - 内置时钟和时间轴控制
   - 自动处理卫星运动、天体力学
   - 支持快进/慢放/回放

3. **真实地球数据**
   - 自带高精度地形、卫星影像
   - 支持Bing Maps、OpenStreetMap等图层
   - 真实星空背景和大气层渲染

4. **大规模场景优化**
   - 可同时渲染数万个卫星（AGI实际案例）
   - 动态LOD（细节层次）系统
   - 高效的场景图管理

#### 适用场景
- **强烈推荐用于**: 需要真实卫星轨道计算、时间回放、大规模节点、专业航天演示
- **实际案例**:
  - NASA Eyes on the Earth
  - AGI Commercial Space Operations Center（数万卫星实时追踪）
  - MathWorks Satellite Scenario Viewer

#### 技术要求
- **学习曲线**: 中等（专业API，但文档完善）
- **Bundle体积**: 较大（~3-5MB gzipped）
- **性能要求**: 需要较好GPU（WebGL 2.0）

#### 代码示例
```javascript
const viewer = new Cesium.Viewer('cesiumContainer');

// 添加卫星
const satellite = viewer.entities.add({
  position: Cesium.Cartesian3.fromDegrees(116.4, 39.9, 550000), // 北京上空550km
  billboard: {
    image: 'satellite.png',
    scale: 0.5
  },
  label: { text: 'Sat-1' },
  path: {
    resolution: 120,
    material: new Cesium.PolylineGlowMaterialProperty({
      glowPower: 0.1,
      color: Cesium.Color.GREEN
    })
  }
});

// 旋转控制（默认支持鼠标拖拽）
viewer.scene.screenSpaceCameraController.enableRotate = true;
```

---

### 方案2: **Globe.GL / Three-Globe** ⭐⭐⭐⭐

#### 基本信息
- **官网**: https://globe.gl/
- **GitHub**: https://github.com/vasturiano/three-globe (2k+ stars)
- **许可证**: MIT
- **底层**: Three.js + WebGL

#### 核心优势
1. **轻量级易用**
   - 基于Three.js，API简洁友好
   - Bundle体积小（~500KB）
   - 上手快，2小时即可实现基本功能

2. **数据可视化能力强**
   - 12+种数据层（弧线、点、路径、热力图等）
   - 支持动画过渡效果
   - 链式API，代码优雅

3. **交互性好**
   - 鼠标拖拽旋转（默认支持）
   - 点击/悬停回调
   - 缩放、平移流畅

4. **专为卫星优化**
   - 官方示例包含"Satellites"演示
   - 粒子层适合卫星节点
   - 弧线层完美展示星间链路

#### 适用场景
- **强烈推荐用于**: 快速原型开发、中小规模卫星网络（<1000节点）、Demo演示
- **实际案例**:
  - 全球航线可视化
  - 卫星覆盖范围展示
  - 网络拓扑演示

#### 技术要求
- **学习曲线**: 低（如果熟悉Three.js则更简单）
- **Bundle体积**: 小（~500KB gzipped）
- **性能要求**: 中等GPU即可

#### 代码示例
```javascript
import Globe from 'globe.gl';

const globe = Globe()
  .globeImageUrl('//unpkg.com/three-globe/example/img/earth-blue-marble.jpg')
  .backgroundImageUrl('//unpkg.com/three-globe/example/img/night-sky.png')
  (document.getElementById('globeViz'));

// 添加卫星节点
globe.pointsData([
  { lat: 39.9, lng: 116.4, alt: 0.1, name: 'Sat-1', color: '#00ff00' },
  { lat: 51.5, lng: -0.1, alt: 0.1, name: 'Sat-2', color: '#00ff00' }
])
.pointAltitude('alt')
.pointColor('color')
.pointLabel('name');

// 添加星间链路
globe.arcsData([
  { startLat: 39.9, startLng: 116.4, endLat: 51.5, endLng: -0.1 }
])
.arcColor(() => '#00ff00')
.arcStroke(2)
.arcDashLength(0.5)
.arcDashGap(0.1)
.arcDashAnimateTime(1000);

// 自动旋转（可选）
globe.controls().autoRotate = true;
globe.controls().autoRotateSpeed = 0.5;
```

---

### 方案3: **Pure Three.js** ⭐⭐⭐

#### 基本信息
- **官网**: https://threejs.org/
- **GitHub**: https://github.com/mrdoob/three.js (100k+ stars)
- **许可证**: MIT
- **定位**: 通用3D渲染库

#### 核心优势
1. **完全可控**
   - 无限制的自定义能力
   - 可集成任何其他库
   - 性能可精细调优

2. **社区资源丰富**
   - 最大的WebGL社区
   - 海量教程和示例
   - 大量第三方插件

3. **轻量灵活**
   - 仅引入需要的模块
   - 可与D3.js等库集成

#### 劣势
- **需要从零实现**
   - 地球纹理、大气层、光照需要手动配置
   - 卫星轨道计算需要额外集成satellite.js
   - 开发周期长（估计2-4周）

#### 适用场景
- **适合**: 需要极致性能优化、高度定制化、学习3D图形编程
- **不适合**: 快速开发、标准地球可视化需求

#### 代码示例
```javascript
import * as THREE from 'three';
import { OrbitControls } from 'three/examples/jsm/controls/OrbitControls';

// 创建场景
const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
const renderer = new THREE.WebGLRenderer();

// 创建地球
const earthGeometry = new THREE.SphereGeometry(1, 64, 64);
const earthTexture = new THREE.TextureLoader().load('earth_texture.jpg');
const earthMaterial = new THREE.MeshPhongMaterial({ map: earthTexture });
const earth = new THREE.Mesh(earthGeometry, earthMaterial);
scene.add(earth);

// 添加旋转控制
const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;

// 渲染循环
function animate() {
  requestAnimationFrame(animate);
  controls.update();
  renderer.render(scene, camera);
}
animate();
```

---

### 方案4: **WebGL Earth** ⭐⭐

#### 基本信息
- **官网**: http://www.webglearth.com/
- **许可证**: Apache 2.0/MIT（双许可）
- **特点**: 轻量级虚拟地球

#### 优劣分析
- ✅ 开源免费
- ✅ 简单易用
- ❌ 社区不活跃（更新较少）
- ❌ 功能较基础
- ❌ 卫星轨道支持需要自己扩展

**结论**: 不推荐（Globe.GL在各方面都优于它）

---

## 🛰️ 卫星轨道计算库

无论选择哪个3D方案，如果需要**真实卫星轨道**，都需要集成轨道计算库：

### satellite.js ⭐⭐⭐⭐⭐

- **GitHub**: https://github.com/shashwatak/satellite-js (2k+ stars)
- **功能**: SGP4/SDP4算法实现，可从TLE数据计算卫星位置
- **许可证**: MIT

```javascript
import * as satellite from 'satellite.js';

// TLE数据（从CelesTrak获取）
const tleLine1 = '1 25544U 98067A   08264.51782528 -.00002182  00000-0 -11606-4 0  2927';
const tleLine2 = '2 25544  51.6416 247.4627 0006703 130.5360 325.0288 15.72125391563537';

// 初始化卫星
const satrec = satellite.twoline2satrec(tleLine1, tleLine2);

// 计算当前位置
const now = new Date();
const positionAndVelocity = satellite.propagate(satrec, now);
const positionEci = positionAndVelocity.position;

// 转换为经纬度
const gmst = satellite.gstime(now);
const positionGd = satellite.eciToGeodetic(positionEci, gmst);

console.log({
  latitude: satellite.degreesLat(positionGd.latitude),
  longitude: satellite.degreesLong(positionGd.longitude),
  altitude: positionGd.height // km
});
```

---

## 🎯 推荐方案选型

### 方案A: **快速实现版（推荐用于MVP）**

**技术栈**: Globe.GL + satellite.js

**理由**:
- ✅ 2-3天即可完成基本功能
- ✅ 代码量少（<300行）
- ✅ 官方卫星示例可直接参考
- ✅ 适合飞腾派硬件性能
- ✅ 支持鼠标拖拽旋转、缩放、平移

**实现难度**: ⭐⭐（低）

**参考示例**: https://globe.gl/example/satellites/

---

### 方案B: **专业展示版（推荐用于最终产品）**

**技术栈**: Cesium.js + satellite.js

**理由**:
- ✅ 真实卫星轨道（自动处理开普勒计算）
- ✅ 时间轴控制（回放、快进）
- ✅ 航天级可视化效果
- ✅ 大规模扩展能力（未来可加入更多卫星）
- ✅ 真实地球纹理和大气层

**实现难度**: ⭐⭐⭐⭐（中高）

**开发周期**: 1-2周

---

### 方案C: **折中方案**

**技术栈**: Three.js + three-globe库（Globe.GL的底层）

**理由**:
- 介于A和B之间的自定义能力
- 可以逐步优化性能
- Three.js技能可复用

**实现难度**: ⭐⭐⭐（中等）

---

## 📦 实现步骤（以Globe.GL为例）

### 第1步: 安装依赖
```bash
npm install globe.gl satellite.js
```

### 第2步: 基础地球（10分钟）
```javascript
import Globe from 'globe.gl';

const globe = Globe()
  .globeImageUrl('//unpkg.com/three-globe/example/img/earth-blue-marble.jpg')
  .bumpImageUrl('//unpkg.com/three-globe/example/img/earth-topology.png')
  .backgroundImageUrl('//unpkg.com/three-globe/example/img/night-sky.png')
  (document.getElementById('globeViz'));
```

### 第3步: 添加卫星节点（30分钟）
```javascript
const satellites = [
  { lat: 0, lng: 0, alt: 0.1, name: 'Sat-1', color: '#00ff00' },
  { lat: 0, lng: 180, alt: 0.1, name: 'Sat-2', color: '#00ff00' }
];

globe.pointsData(satellites)
  .pointAltitude('alt')
  .pointRadius(0.5)
  .pointColor('color')
  .pointLabel('name');
```

### 第4步: 添加星间链路（30分钟）
```javascript
const links = [
  { startLat: 0, startLng: 0, endLat: 0, endLng: 180 }
];

globe.arcsData(links)
  .arcColor(() => '#00ff00')
  .arcStroke(2);
```

### 第5步: 集成真实轨道（2小时）
```javascript
import * as satellite from 'satellite.js';

// 使用satellite.js计算实时位置
function updateSatellitePositions() {
  const now = new Date();
  const updatedSatellites = satellites.map(sat => {
    const posVel = satellite.propagate(sat.satrec, now);
    const posGd = satellite.eciToGeodetic(posVel.position, satellite.gstime(now));
    return {
      ...sat,
      lat: satellite.degreesLat(posGd.latitude),
      lng: satellite.degreesLong(posGd.longitude),
      alt: posGd.height / 6371 // 转换为地球半径单位
    };
  });
  globe.pointsData(updatedSatellites);
}

setInterval(updateSatellitePositions, 1000); // 每秒更新
```

---

## 🔥 Cesium.js 完整示例

```javascript
import * as Cesium from 'cesium';
import * as satellite from 'satellite.js';

const viewer = new Cesium.Viewer('cesiumContainer', {
  terrainProvider: Cesium.createWorldTerrain(),
  imageryProvider: new Cesium.IonImageryProvider({ assetId: 3845 })
});

// TLE数据
const tle = {
  line1: '1 25544U 98067A   08264.51782528 -.00002182  00000-0 -11606-4 0  2927',
  line2: '2 25544  51.6416 247.4627 0006703 130.5360 325.0288 15.72125391563537'
};

const satrec = satellite.twoline2satrec(tle.line1, tle.line2);

// 创建卫星实体
const satelliteEntity = viewer.entities.add({
  name: 'ISS',
  position: new Cesium.CallbackProperty(() => {
    const posVel = satellite.propagate(satrec, new Date());
    const posGd = satellite.eciToGeodetic(posVel.position, satellite.gstime(new Date()));
    return Cesium.Cartesian3.fromDegrees(
      satellite.degreesLong(posGd.longitude),
      satellite.degreesLat(posGd.latitude),
      posGd.height * 1000 // 转换为米
    );
  }, false),
  billboard: {
    image: 'satellite.png',
    scale: 0.5
  },
  path: {
    resolution: 120,
    material: new Cesium.PolylineGlowMaterialProperty({
      glowPower: 0.1,
      color: Cesium.Color.YELLOW
    }),
    width: 2
  }
});

// 跟踪卫星
viewer.trackedEntity = satelliteEntity;
```

---

## 💰 成本对比

| 方案 | 开发时间 | 学习成本 | 代码量 | 运行性能 | 可维护性 |
|------|---------|---------|--------|---------|---------|
| Globe.GL | 2-3天 | 低 | ~300行 | 优秀 | 高 |
| Cesium.js | 1-2周 | 中 | ~500行 | 优秀 | 中 |
| Three.js | 2-4周 | 高 | ~1000行 | 可调优 | 中 |
| 当前2D Canvas | - | - | ~550行 | 一般 | 低 |

---

## 🎨 效果对比

### 当前2D方案
- ❌ 无法旋转查看不同角度
- ❌ 大陆位置简化失真
- ❌ 地面站位置错误（在地球外）
- ❌ 无真实光照和大气层
- ✅ 性能好（2D渲染）

### 3D方案（Globe.GL/Cesium）
- ✅ 鼠标拖拽360°旋转
- ✅ 真实地球纹理和地形
- ✅ 真实卫星轨道计算
- ✅ 大气层、星空背景、光照效果
- ✅ 缩放、平移流畅
- ⚠️ 需要GPU加速（飞腾派满足要求）

---

## 🚀 实施建议

### 阶段1: 快速验证（本周）
1. 使用Globe.GL实现基础3D地球
2. 手动设置7个节点位置（模拟轨道）
3. 绘制星间链路
4. 验证飞腾派性能

**预期成果**: 可旋转的3D地球 + 7个节点 + 链路

---

### 阶段2: 真实轨道（下周）
1. 集成satellite.js
2. 使用TLE数据计算真实轨道
3. 实时更新节点位置
4. 添加轨道轨迹线

**预期成果**: 卫星按真实轨道运行

---

### 阶段3: 完善功能（后续）
1. 添加时间控制面板（快进/暂停/回放）
2. 链路可见性算法（根据卫星视距判断）
3. 统计面板集成
4. 性能优化

---

## 📚 参考资源

### Globe.GL
- 官方文档: https://github.com/vasturiano/globe.gl
- 卫星示例: https://globe.gl/example/satellites/
- API参考: https://github.com/vasturiano/three-globe#api-reference

### Cesium.js
- 官方教程: https://cesium.com/learn/cesiumjs-learn/
- 卫星教程: https://cesium.com/blog/2016/11/10/visualizing-tle-data/
- Sandcastle示例: https://sandcastle.cesium.com/

### satellite.js
- GitHub: https://github.com/shashwatak/satellite-js
- TLE数据源: https://celestrak.org/
- 文档: https://github.com/shashwatak/satellite-js#usage

### Three.js
- 官方教程: https://threejs.org/docs/
- 地球示例: https://threejs.org/examples/#webgl_earth

---

## 🎯 最终推荐

**对于SAGIN项目**，我强烈推荐：

### 第一选择: **Globe.GL**
**理由**:
1. 快速实现（2-3天完成基础功能）
2. 代码简洁易维护
3. 官方卫星示例完美匹配需求
4. 性能优秀（适合飞腾派）
5. MIT许可证，无任何限制

### 备选方案: **Cesium.js**
**适用于**: 需要专业级效果、时间回放、真实轨道计算的场景

---

## 🔧 当前2D方案的问题修复

在实施3D方案之前，可以快速修复当前2D版本的地面站位置问题：

**修改文件**: `sagin_web_visualizer.py`

```python
# 第25行，修改为:
ORBIT_RADIUS = {
    'satellite': 280,
    'aircraft': 180,
    'ground': 85  # 从110改为85（地球半径80+5像素偏移）
}
```

这样地面站就会显示在地球表面，而不是地球外。

---

**报告完成时间**: 2025-11-14
**预计3D实施周期**: 2-7天（取决于选择方案）
