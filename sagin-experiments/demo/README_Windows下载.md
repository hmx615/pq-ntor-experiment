# Windows 下载资源 - 操作步骤

## 📋 快速步骤

### 第1步：在Windows浏览器中下载文件

**方法A - 最简单（推荐）**：

1. 在Windows中打开浏览器（Chrome/Edge）

2. 访问这两个链接，浏览器会自动下载：

   **链接1**:
   ```
   https://cdn.jsdelivr.net/npm/three@0.154.0/build/three.min.js
   ```

   **链接2**:
   ```
   https://cdn.jsdelivr.net/npm/globe.gl@2.31.0/dist/globe.gl.min.js
   ```

3. 下载完成后，文件可能在 `C:\Users\你的用户名\Downloads\`

---

### 第2步：重命名文件

将下载的文件改名为：
- `three.min.js`
- `globe.gl.min.js`

---

### 第3步：复制到WSL目录

**方法1 - 使用文件资源管理器（推荐）**：

1. 按 `Win + E` 打开文件资源管理器

2. 在地址栏输入并回车：
   ```
   \\wsl$\Ubuntu\home\ccc\pq-ntor-experiment\sagin-experiments\demo\
   ```

3. 将两个JS文件拖拽到这个文件夹

**方法2 - 使用命令**：

在Windows PowerShell中运行：
```powershell
cd $HOME\Downloads
Copy-Item three.min.js \\wsl$\Ubuntu\home\ccc\pq-ntor-experiment\sagin-experiments\demo\
Copy-Item globe.gl.min.js \\wsl$\Ubuntu\home\ccc\pq-ntor-experiment\sagin-experiments\demo\
```

---

### 第4步：验证文件

在WSL终端中运行：
```bash
ls -lh /home/ccc/pq-ntor-experiment/sagin-experiments/demo/*.js
```

应该看到：
```
-rw-r--r-- 1 user user 600K  globe.gl.min.js
-rw-r--r-- 1 user user 150K  three.min.js
```

---

### 第5步：访问3D演示

在Windows浏览器中访问：
```
http://localhost:8080/3d_globe_local.html
```

✅ 完成！应该能看到3D地球和模型了

---

## 🔍 故障排查

### 问题1：找不到 `\\wsl$` 路径
**解决**：确保WSL正在运行，在WSL终端运行任意命令即可

### 问题2：文件大小只有几KB
**解决**：可能下载了错误的页面，请：
1. 在浏览器中右键点击链接
2. 选择"另存为"或"Save Link As"
3. 确保保存为 `.js` 文件

### 问题3：还是显示"加载失败"
**解决**：
1. 在WSL中检查文件：`ls -lh /home/ccc/pq-ntor-experiment/sagin-experiments/demo/*.js`
2. 确认文件大小合理（不是0或几KB）
3. 刷新浏览器页面（Ctrl+F5强制刷新）

---

## 📦 文件说明

| 文件 | 大小 | 用途 |
|------|------|------|
| three.min.js | ~600KB | Three.js 3D引擎 |
| globe.gl.min.js | ~150KB | Globe.GL 地球库 |

---

## 🎯 下一步

文件下载后：
- 访问 `http://localhost:8080/3d_globe_local.html`
- 应该能看到：
  - 🌍 3D地球（不旋转）
  - 🛰️ 卫星模型（黄色立方体+太阳能板）
  - ✈️ 无人机模型（青色四面体）
  - 📡 地面站模型（绿色圆锥）
  - 📏 金黄色国界线

---

创建时间：2025-11-14
