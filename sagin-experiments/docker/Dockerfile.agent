# Dockerfile for Node Agent (Pi-1 to Pi-6)
FROM python:3.11-slim

WORKDIR /app

# 安装依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY node_agent.py .

# 运行Node Agent
CMD ["python", "node_agent.py"]
